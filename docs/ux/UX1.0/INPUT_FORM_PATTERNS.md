# Input form patterns

Every field has label, engineering unit, valid domain, provenance status and help tied to C1. Basic mode shows normal engine data. Advanced sections expose measurement stations, uncertainty/covariance, dataset hashes, mesh profile and diagnostics. Arrays use tables/plots with import and station preview. SI is stored internally; display conversion never rewrites semantics. Save creates a project revision; unsaved changes are explicit.
