# Reproducibility contract

Pinned lockfiles, deterministic seeds, canonical serialization, immutable fixtures and exact commands are mandatory. Runs publish source/input/environment/log/raw/derived/reference hashes atomically. Partial outputs remain invalid. Changing a fixture or threshold requires BCR; regenerating a reference records method and error estimate. No network access is required during a numerical run after dependencies/data are resolved.
