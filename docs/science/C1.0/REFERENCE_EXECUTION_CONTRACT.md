> **Normative — C1.0-R3** · Status: `SCIENTIFIC_IMPLEMENTATION_BASELINE_FROZEN`

# Reference execution details — REF-001

These definitions complete the reference construction fields in EXECUTABLE_VALIDATION_FIXTURES.json. They are mandatory future verification, not newly executed CFD. Previously recovered references and their failed error allocations remain evidence, never promoted by this revision.

For coupled reference A use conservative finite-difference WENO5-JS on uniform point states Q=AU. Split F±=(AF±alpha Q)/2 with alpha=max(abs(u)+a) over the full domain at each RK stage. Normalize each conserved component by its fixture physical scale before the following scalar formulas. Positive-flux candidates at i+1/2 are q0=(2fi−2−7fi−1+11fi)/6, q1=(−fi−1+5fi+2fi+1)/6, q2=(2fi+5fi+1−fi+2)/6. Smoothness indicators are beta0=13/12(fi−2−2fi−1+fi)^2+1/4(fi−2−4fi−1+3fi)^2; beta1=13/12(fi−1−2fi+fi+1)^2+1/4(fi−1−fi+1)^2; beta2=13/12(fi−2fi+1+fi+2)^2+1/4(3fi−4fi+1+fi+2)^2. Weights proportional to (.1,.6,.3)/(1e−12+beta)^2, normalized by their sum; negative flux uses the mirrored stencil. No characteristic projection or flux B. Geometric source is p times the centered sixth-order area derivative (−A_i−3+9A_i−2−45A_i−1+45A_i+1−9A_i+2+A_i+3)/(60 dx); at smooth segment boundaries use analytic derivative of that segment instead. W2/heat/source quadrature is pointwise. Boundary flux is BC evaluated by independently coded wave curves; constant trace ghosts fill three cells, boundary face flux explicitly replaces WENO there. Physical station samples use cubic interpolation on the one-sided smooth stencil, reducing to linear if its stencil crosses a detected pressure extremum; report both samples as an observation-discretization uncertainty. No filtering of temporal traces.

Reference B uses finite-volume exact cell averages, piecewise-constant states and a full exact NASA Riemann solver at interior faces, with Hugoniot/rarefaction pressure matching and wave sampling as BC-003 mirrored for the left wave; advected species come from the contact donor. Integrate source p_i DeltaA/dx and physical W2 over cells; DOP853 integrates normalized inventories with the catalogue tolerances and event alignment. Its different first-order spatial operator is deliberately an independent convergence check, not a production candidate. Use exact boundary wave equations but independently written bracketing and thermodynamic code. It may require more refinement than A. Do not claim a reference PASS unless BOTH allocated estimates and cross-method agreement qualify. An inadmissible RK/reference stage rejects and halves the whole trial (16 retries); no clipping or state repair. If the reference cannot qualify within an execution budget, report REFERENCE_NOT_QUALIFIED, preserve outputs, and request a BCR before changing the reference or acceptance.

For reference A geometric point-state versus candidate cell-average comparisons, integrate the reference reconstructed field over each candidate cell before computing norms. A station observable is a point value in both, not one solver's cell average versus the other's nodal pressure. Nonuniform physical segments require mesh vertices aligned to geometric/source breakpoints. Constant-area coupled fixtures remove the geometric difference; VAL-011 checks geometry separately.

ESTABLISHED_PUBLISHED_METHOD: Jiang & Shu (1996), *Efficient Implementation of Weighted ENO Schemes*, DOI [10.1006/jcph.1996.0130](https://doi.org/10.1006/jcph.1996.0130), and exact Euler wave sampling [Clawpack](https://www.clawpack.org/riemann_book/html/Euler.html). The normalized regularizer, observation rule, reference pair and error allocations are DINO2NEXT_SPECIFIC_ADAPTATION. No claim of a published theorem about the entire NASA/engine boundary follows from these references.

## R3 thermochemistry reference identity

REF-001 and every NASA reference use TI-001 derived thermodynamics, including entropy wave curves, Hugoniot, enthalpy inversion and source temperature bounds. Each reference implements that definition independently of the candidate. RAW evidence cannot qualify the derived representation automatically. TI-004 specifies Cantera/high-precision cross-checks; TI-005 retains all assigned reruns and budgets.

## C1.0-R4 bounded S06 decisions

BCR-S06-VERIFICATION-CONTRACT SV-008/009/010/027 is the normative companion for the four affected gates. It preserves original failed evidence, all unrelated physics, amplitude/error budgets and accepted TI-001..005. SV-009 changes only smooth acoustic limiting and requires affected kernel regressions. SV-008 preserves the original error window despite added computational guards. SV-010 separates directional reflection signals, qualifying a reference-defined observation mask and retaining absolute checks on every sample. SV-027 retains original levels and threshold and adds .000125s; reference qualification is not a two-run discrepancy mislabeled as certification. No implementation, numerical, experimental or predictive acceptance follows from document existence.
