# Portable software equivalence evidence

This capsule supports two storage/performance changes used for the directed N1600 kernel experiments. Neither changes the physical contract or establishes S06 acceptance.

The sparse nonnegative constituent-sum optimization is published in implementation PR8 at2d04c16fc77b603596f7db04a8ed7d35953afa17. Its tree6654f11dd9e564aebfab2f0fd3bf94216415fc59 is identical to the locally executed eadf7ba1f1f3208076347e38ecaac056f2e8755b. ROOT authored the runtime change; s03_runtime_builder authored its tests/pilots; bcr_science_review independently approved the exact sum identity after70unit/contract tests,40328rows and323pilot arrays/traces.

The experimental streaming adapter is authored by s03_runtime_builder and independently reviewed by ROOT. It preserves the original canonical execution arithmetic,101targets,retry paths,compensated ledgers and all diagnostics, replacing only accumulated JSON lists with gzip JSONL streams. Two pilots compare raw arrays and all decoded traces, including actual limiters and an explicitly injected software-only rejection. Hash and count corruption checks also passed. It consumes the separately qualified N1600 observation helper and original assessment operators. Full trajectories are separate evidence and are not included as completed runs in this capsule.

The ZIP contains only paths under `artifacts/S06-exact-sums/`, with exact file hashes and sizes in manifest.json. It includes original/candidate arrays, diagnostic traces, scripts, independent reviews and test logs. It does not require the original E: paths: obtain a checkout of the implementation commit above, verify the ZIP/member hashes, and extract these relative artifact paths there. Set PYTHONPATH to that checkout's src. `python artifacts/S06-exact-sums/reviewer_check.py` and `python artifacts/S06-exact-sums/streaming/independent_check.py` replay the independent stored-evidence audits. The pilot scripts rerun bounded software trajectories and write their own artifact outputs; preserve the capsule before rerunning.

For the full acoustic experiment, the runner takes an explicit `--observation` path to acoustic/observation.py from this BCR checkout, `--cfl`(.05 or.1), `--epsilon`(1e-5 or5e-6), and a distinct output directory. Source identities are recorded and checked across execution. A completed execution record with pending assessment, an interruption, or a storage error is never PASS. CPU/wall-time and memory measurements are recorded separately, including concurrent execution context.
