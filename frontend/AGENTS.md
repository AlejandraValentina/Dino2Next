# Frontend agent rules

The frontend performs schema validation, configuration, execution control and visualization only. It never computes authoritative pressure, flow, torque, power, scavenging, combustion, losses or convergence. No fake scientific results. Every editable field maps through schema/domain/application/solver/provenance. Use the traditional tree, progressive disclosure, accessible controls and explicit units.
