# Validation agent rules

Fixtures and thresholds are immutable without BCR. Candidate cannot be its oracle. Preserve raw data, commands, reference independence, uncertainty and hashes. A failed heavy gate remains failed; do not relax acceptance or silently replace artifacts.
