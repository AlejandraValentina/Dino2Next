# Contributing

Select one implementation scope, create `codex/SXX-short-name`, follow all referenced contracts, run its acceptance commands, preserve artifacts and open a focused PR. Scientific changes require a BCR. A reviewer assesses the diff against contracts rather than the builder narrative. Main must remain green.
