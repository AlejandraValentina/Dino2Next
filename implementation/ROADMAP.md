# Implementation roadmap

Execute scopes in the dependency graph. Science-first foundation: units/provenance → geometry/thermo → 0D/1D numerics → boundaries/T3/W2 → engine closures → convergence/results → application/frontend → integrated verification. Heavy verification runs at milestones, not every PR. No scope may claim numerical or predictive completion ahead of its gates.
