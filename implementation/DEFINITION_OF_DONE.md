# Definition of done

A scope is done when allowed files alone changed, required interfaces and failures match contracts, tests and acceptance commands pass, artifacts/provenance are preserved, independent review finds no authority violation, CI is green and unsupported behavior stays explicit. A completed code path with an outstanding mandatory gate is not scientifically verified.
